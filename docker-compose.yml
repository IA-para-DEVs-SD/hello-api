version: '3.8'

services:
  api:
    build: .
    container_name: hello-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/code/app
    environment:
      - ENVIRONMENT=development
    restart: unless-stopped
